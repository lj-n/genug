import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	variants
} from './button.svelte';

export {
	Root as Button,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	variants as buttonVariants
};
