engine-strict=true
# Supply-chain cooldown: refuse to resolve versions published <3 days ago.
# Requires npm >=11.17 (see engines) — older npm silently ignores this key.
# Urgent fresh release needed? Add it to min-release-age-exclude, don't lower this.
min-release-age=3
